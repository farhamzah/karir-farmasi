import { Head, Link, useForm } from '@inertiajs/react';import type { FormEvent } from 'react';import Brand from '../../components/Brand';
export default function Login(){const f=useForm({email:'',password:''});return <><Head title="Login Perusahaan"/><main className="company-auth app-surface"><header><Brand href="/" context="MITRA PERUSAHAAN"/><Link className="ghost-button" href="/company/register">Daftar perusahaan</Link></header><section className="company-login-wrap"><form className="company-auth-card" onSubmit={(e:FormEvent)=>{e.preventDefault();f.post('/company/login')}}><p className="eyebrow">PORTAL REKRUTER</p><h1>Masuk ke ruang talenta.</h1><p>Akses pencarian tersedia untuk perusahaan yang sudah diverifikasi.</p><label>Email kerja<input type="email" value={f.data.email} onChange={e=>f.setData('email',e.target.value)} required/></label><label>Kata sandi<input type="password" value={f.data.password} onChange={e=>f.setData('password',e.target.value)} required/></label>{Object.values(f.errors).map((x,i)=><p className="field-error" key={i}>{x}</p>)}<button className="primary-button" disabled={f.processing}>Masuk</button></form></section></main></>}
